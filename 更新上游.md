# 更新上游.md —— 原作者更新后，怎么让汉化版跟上

## 0. 先搞清楚一件事：为什么不能「无脑跟进」

本仓库用的是**覆盖层（overlay）**模式：

- 仓库里只放我们**改过的文件**（目前 64 个）；
- CI 每次构建时，先克隆上游到 `.github/upstream-commit.txt` 里锁定的提交，再把我们的文件**整份盖**上去。

「整份盖」意味着：**如果上游恰好也改了我们盖的某个文件，直接跟进会把上游的修复静默吞掉**。
我们改的文件里有 `menu_common.cpp`（324KB，上游最活跃的文件）和 `OptiScaler.ini`，
所以这件事**经常会发生**，必须区分对待。

因此自动化分两条路：

| 情况 | 上游改的文件 | 处理方式 |
| --- | --- | --- |
| **A. 无重叠** | 不包含我们改过的任何文件 | ✅ **全自动**：更新锁定版本 → 自动构建 → 自动更新 Release，你什么都不用做 |
| **B. 有重叠** | 至少包含一个我们改过的文件 | ⚠️ **自动开 Issue 通知你**，需要人工把汉化重新施加到新版本 |

`check-upstream.yml` 每天 11:00（北京时间）自动检查一次，也可以在
Actions 页面手动触发 `检查上游更新`（可勾选「只检测不修改」用来验证流程）。

---

## 1. 情况 A：无重叠（全自动，无需操作）

流程会自动：

1. 把 `.github/upstream-commit.txt` 改成上游新提交并推送到 main；
2. 显式触发一次 `Build OptiScaler CN`（因为用 `GITHUB_TOKEN` 推送不会触发其它 workflow）；
3. 构建完成后自动更新 Release 上的两个附件。

你只需要等构建变绿，然后去 Releases 页面下载新包即可。

> 顺带说明：跟的是上游 **master 分支的提交**，不是 Release 标签——
> 上游的 Release（v0.9.4）远落后于 master（v10.0.0-dev），跟 Release 会拿不到新功能。

---

## 2. 情况 B：有重叠（需要人工，但大部分能自动化）

### 2.1 会发生什么

`check-upstream.yml` 会开一个标题带 `upstream-sync` 的 Issue，里面列出：

- 锁定版本 → 上游最新版本；
- 上游新增的提交数、改动文件数；
- **重叠文件清单**（必须人工处理的）。

之后如果又出现重叠，会在**同一条 Issue** 下追加评论，不会刷屏。

### 2.2 人工处理的完整步骤

#### 第 1 步：拿到上游新版本的源码

本机 `git clone` / `git fetch` 走不通（代理拦 `github.com`），但 `api.github.com` 是通的，
所以用仓库里自带的工具下载指定提交的源码包：

```bash
cd /d/插件/OptiScaler-CN
C:/Users/liang/.workbuddy/binaries/python/versions/3.13.12/python.exe tools/fetch_upstream.py <新的完整SHA>
# 结果解压到 build/upstream-<SHA>/
```

#### 第 2 步：把汉化补丁应用上去（大部分自动完成）

每次构建都会生成一份「汉化补丁」，它描述的是
「上游锁定版本 → 我们的汉化版本」的全部差异，作为 Release 的第三个附件
`localization.patch` 一起发布，**永远是最新的**。

```bash
# 在第 1 步解压出来的上游目录里应用补丁
cd build/upstream-<SHA>/
git apply --reject --whitespace=nowarn ../../zh-CN/构建工具/localization.patch
```

- `--reject` 的含义：**能合的自动合，合不上的写进 `.rej` 文件**，不会整份失败。
- 结束后用 `find . -name "*.rej"` 找出所有被拒绝的片段，逐个人工合并。
  这些就是上游和我们「改到同一处」的地方，通常不多。

> 补丁里**刻意不含** `OptiScaler/menu/font/NotoSansSC_Subset.h`（1.4MB 的字体数据），
> 因为它是工具生成的，第 4 步重新生成即可。

#### 第 3 步：重跑字符串替换式汉化工具（自动适应新版本）

这几个工具是「英文原文 → 中文译文」的精确替换，**上游换了版本也能直接重跑**，
没匹配到的会明确列出来（那通常说明上游改了措辞，需要你补译）：

```bash
cd /d/插件/OptiScaler-CN
P=C:/Users/liang/.workbuddy/binaries/python/versions/3.13.12/python.exe

$P tools/i18n_ini.py --apply          # INI 注释（约 732 行）
$P tools/i18n_toasts.py --apply       # 运行时提示弹窗（22 条）
$P tools/i18n_ui_labels.py --apply    # 菜单控件标签
$P tools/i18n_linux_sh.py --apply     # setup_linux.sh
bash -n <上游目录>/setup_linux.sh     # 语法校验
```

#### 第 4 步：菜单文案（menu_common.cpp）——唯一需要人工的地方

这一份约 1000 处文案是按**字节偏移**替换的，上游一动它就错位，无法自动重放。
推荐做法：

1. 用文本对比工具（VS Code：右键 → 「选择以进行比较」）对比
   `上游新版的 menu_common.cpp` vs `我们现在的 menu_common.cpp`；
2. 把上游新增的**非文案**改动（新参数、新逻辑、Bug 修复）挑过来；
3. 上游新增的英文文案，用这两个工具找出来补译：
   ```bash
   $P tools/find_ui_strings.py     # 列出还没汉化的控件标签（应为 0 条才干净）
   $P tools/find_toasts.py         # 列出还没汉化的弹窗文案（应为 0 条才干净）
   ```
4. 改完补进 `tools/i18n_ui_labels.py` / `i18n_toasts.py` 的替换表里，
   下次上游更新就能自动处理了。

> ⚠️ 注意：不要把 `LOG_*` 日志文案一起翻——那不是界面，翻了对玩家没意义还污染日志。

#### 第 5 步：重新生成中文字体子集（必做，顺序很重要）

**所有源码改完之后**再跑，否则新文案的字会缺字显示成方块：

```bash
F=C:/Users/liang/.workbuddy/binaries/python/envs/default/Scripts/python.exe
$F tools/make_font_subset.py
$F tools/verify_font.py          # 必须输出「字形覆盖完整，没有缺字」
```

#### 第 6 步：静态自检 + 上传

```bash
$P tools/syntax_check.py                 # 括号/引号配平
$P tools/find_toasts.py                  # 弹窗 0 条遗漏
$P tools/find_ui_strings.py              # 控件标签只剩该保留英文的那 16 条

# 上传（会自动触发一次构建，构建完自动更新 Release）
$P tools/publish_overlay.py --token-file build/cred.txt            # 干跑看清单
$P tools/publish_overlay.py --token-file build/cred.txt --apply    # 真正上传
```

CI 里新增的探针会验证弹窗文案真的编进了 DLL，所以「假成功」会被拦下来。

---

## 3. 一句话总结

- **无重叠 → 全自动**，你什么都不用做。
- **有重叠 → 看 Issue**，按上面 6 步走；其中第 2、3 步是自动的，真正要动脑的只有
  「menu_common.cpp 里上游新增的文案」这一小块。

## 4. 以后可以再升级的地方

现在补丁是「上游锁定版 → 汉化版」的整段 diff，应用时靠上下文匹配。如果以后觉得
人工合并 `.rej` 太麻烦，可以升级成**三方合并**（`git apply --3way`），它需要同时提供
上游旧版对象库，能把上游的改动和我们的改动在**同一文件的不同位置**都自动合上，
只把真正冲突的地方留给人。代价是要维护上游的历史对象，复杂度更高，目前先不做。
